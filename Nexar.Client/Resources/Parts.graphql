query Parts($id: ID!) {
  desComponentById(id: $id) {
    manufacturerComponents {
      ...MyPart
    }
  }
}

fragment MyPart on DesManufacturerComponent {
  name
  partNumber
  supplierComponents {
    name
    partNumber
    prices {
      currency
      price
      breakQuantity
    }
  }
}
